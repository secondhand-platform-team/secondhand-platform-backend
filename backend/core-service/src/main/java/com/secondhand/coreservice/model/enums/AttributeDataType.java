package com.secondhand.coreservice.model.enums;

/**
 * Enum định nghĩa kiểu dữ liệu cho các thuộc tính động của category
 */
public enum AttributeDataType {
    STRING,
    NUMBER,
    INTEGER,
    BOOLEAN,
    DATE,
    ENUM,
    JSON
}
